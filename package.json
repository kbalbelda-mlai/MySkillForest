{
  "name": "the-skill-forest",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare:owner": "node scripts/prepareOwnerMode.mjs",
    "prepare:public": "node scripts/preparePublicMode.mjs",
    "sync:evidence": "node scripts/syncEvidence.mjs",
    "dev": "npm run prepare:owner && astro dev",
    "build": "npm run prepare:public && npm run sync:evidence && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/node": "^11.0.2",
    "astro": "^7.1.0",
    "papaparse": "^5.5.4"
  },
  "devDependencies": {
    "@types/papaparse": "^5.5.2"
  }
}
